<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <li><%= task.status ? "[X]" : "[ ]" %> <%= task.name %> <%= link_to "details", task_path(task) %></li>
  <% end %>
</ul>

<%= link_to "Add task", new_task_path %>
